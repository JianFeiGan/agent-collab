interface:
  display_name: "Agent Collab"
  short_description: "Repo-specific patterns and workflows for agent-collab"
  default_prompt: "Use the agent-collab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true